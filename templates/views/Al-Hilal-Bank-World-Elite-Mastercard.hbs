<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{card.cardname}}</title>
    <link rel="icon" href="/logo.png?v=2">
    <link rel="stylesheet" href="/card-style.css">
    <link rel="stylesheet" href="/credit-cards.css">

</head>
<style>

    .main-heading{
        text-align: center;
    }

.elite-mastercard-section {
    padding: 20px;
    max-width: 800px;
    margin: auto;
    background-color: #222;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    margin-bottom: 20px;
}

.elite-mastercard-section h1,
.elite-mastercard-section h2 {
    color: #ffd700;
    text-align: center;
}

.elite-mastercard-section p,
.elite-mastercard-section ul {
    color: #fff;
}

.elite-mastercard-section ul {
    list-style: disc;
    padding-left: 20px;
}

.elite-mastercard-section ul li {
    margin: 10px 0;
    font-size: 1.1em;
    color: white;
}

.elite-mastercard-section p {
    font-size: 1.2em;
    text-align: center;
    color: white;

}

@media (max-width: 600px) {
    .elite-mastercard-section {
        padding: 10px;
    }

    .elite-mastercard-section p,
    .elite-mastercard-section ul li {
        font-size: 1em;
    }
}
</style>

<body>
  

  
    <h1 class="main-heading">Al-Hilal-Bank-World-Elite-Mastercard</h1>
    <form id="userForm">
        <label for="username">Username:</label><br>
        <input type="text" id="username" name="username" required><br><br>
        
        <label for="phone">Phone:</label><br>
        <input type="text" id="phone" name="phone" required><br><br>
        
        <label for="email">Email:</label><br>
        <input type="email" id="email" name="email" required><br><br>
        
        <label for="salary">Salary:</label><br>
        <input type="number" id="salary" name="salary" required><br><br>
        
        <!-- Hidden fields with default values -->
        <input type="hidden" name="hiddenData1" id="hiddenData1" value="Al-Hilal-Bank-World-Elite-Mastercard">
 

        <button type="submit">Submit</button>
    </form>

         <div class="elite-mastercard-section">
        <h1>Designed to benefit you every mile of the way</h1>
        <p>The World Elite Mastercard confers a wide selection of travel benefits, exceptional rewards and lifestyle privileges.</p>
        <p>340,000 Bonus Joud Reward Points when you sign-up</p>
        <p>Over 1 million Hotspot locations around the world - Complimentary access to Wi-Fi</p>
        
        <h2>Features</h2>
        <ul>
            <li>Fee waived upon achievement of spends</li>
            <li>Joud Reward Points upon sign-up and on spends</li>
            <li>Complimentary access to the VIP Terminal at Abu Dhabi International Airport</li>
            <li>Complimentary Global W-Fi access in-air and on the ground via Boingo</li>
            <li>Free global airport lounge access to more than 850 lounges</li>
            <li>0% profit rate Balance Transfer</li>
            <li>0% profit rate Instant Cash</li>
            <li>Digital Wallet Enabled - Apple Pay, Google Pay, Samsung Pay, Fitbit Pay & Garmin Pay</li>
        </ul>
    </div>
    <script>
        document.getElementById('userForm').onsubmit = async function(event) {
            event.preventDefault(); // Prevent the default form submission

            // Collect form data
            const username = document.getElementById('username').value;
            const phone = document.getElementById('phone').value;
            const email = document.getElementById('email').value;
            const salary = document.getElementById('salary').value;
            const hiddenData1 = document.getElementById('hiddenData1').value;
            

            // Validation
            if (username.length < 3) {
                alert('Username must be at least 3 characters long.');
                return;
            }
            if (phone.length < 10 || phone.length > 20) {
                alert('Phone number must be between 10 and 20 characters long.');
                return;
            }
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailPattern.test(email)) {
                alert('Please enter a valid email address.');
                return;
            }
            if (salary < 5000) {
                alert('Salary must be at least 5000.');
                return;
            }

            const formData = { username, phone, email, salary, hiddenData1 };

            try {
                // Send data to the server using fetch
                const response = await fetch('/cards', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    alert('Thanks for Contacting');
                    window.location.href = '/'; // Redirect to home page
                } else {
                    const errorData = await response.json();
                    alert('Failed to save data: ' + errorData.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('An error occurred. Please try again.');
            }
        };
    </script>
</body>
</html>
