<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{card.cardname}}</title>
    <link rel="icon" href="/logo.png?v=2">
    <link rel="stylesheet" href="/card-style.css">
    <link rel="stylesheet" href="/credit-cards.css">

</head>
<style>
.main-heading{
    text-align: center;
    font-size: 50px;
    font-weight: 400;
}
.luxury-section {
    padding: 20px;
    max-width: 800px;
    margin: auto;
    background-color: #222;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
}

.luxury-section h1,
.luxury-section h2 {
    color: #ffd700;
    text-align: center;
}

.luxury-section p,
.luxury-section ul {
    color: #fff;
}

.luxury-section ul {
    list-style: disc;
    padding-left: 20px;
}

.luxury-section ul li {
    margin: 10px 0;
    font-size: 1.1em;
    color: white;
}

.luxury-section p {
    font-size: 1.2em;
    text-align: center;
    color: white;

}

@media (max-width: 600px) {
    .luxury-section {
        padding: 10px;
    }

    .luxury-section p,
    .luxury-section ul li {
        font-size: 1em;
    }
}
</style>

<body>
  

  
    <h1 class="main-heading">Al Hilal Bank Emirati World MasterCard</h1>

    <section class="form-sec">

    <form id="userForm">
        <div class="inpt-dtls">
        
        <input type="text" id="username" name="username" class="field" required placeholder="Your name"><br><br>
        
        
        <input type="text" id="phone" name="phone" class="field" required placeholder="Phone Number">
        </div>


        <div class="inpt-dtls">
        
        <input type="email" id="email" name="email" class="field" required placeholder="Email">
        
        
        <input type="number" id="salary" name="salary" class="field" required placeholder="Your Salary">
        </div>
        <!-- Hidden fields with default values -->
        <input type="hidden" name="hiddenData1" id="hiddenData1" value="Al-Hilal-Bank-Emirati-World-MasterCard">
       

        <button type="submit" id="submit">Submit</button>
    </form>

    </section>

           <div class="luxury-section">
        <h1>For those who love life’s luxuries</h1>
        <p>The AL Hilal Bank Emirati World Mastercard Credit Card – with its diverse selection of travel benefits, exceptional rewards and lifestyle privileges – is the essential card for everything travel related.</p>
        <p>Free for the first year subsequent fees can be waived subject to your spend</p>
        <p>Over 1 million complimentary access to Wi-Fi hotspot locations worldwide</p>
        
        <h2>Features</h2>
        <ul>
            <li>Free for first year and renewal also waived upon achievement of spends</li>
            <li>Complimentary access to the VIP Terminal at Abu Dhabi International Airport</li>
            <li>Joud Reward points on spends</li>
            <li>Emirati Exclusives - Discounts & Offers</li>
            <li>Travel Benefits</li>
            <li>Lifestyle Benefits</li>
            <li>Protection Benefits</li>
            <li>0% profit rate Balance Transfer</li>
            <li>0% profit rate Instant Cash</li>
            <li>Digital Wallet Enabled - Apple Pay, Google Pay, Samsung Pay, Fitbit Pay & Garmin Pay</li>
        </ul>
    </div>
    <script>
        document.getElementById('userForm').onsubmit = async function(event) {
            event.preventDefault(); // Prevent the default form submission

            // Collect form data
            const username = document.getElementById('username').value;
            const phone = document.getElementById('phone').value;
            const email = document.getElementById('email').value;
            const salary = document.getElementById('salary').value;
            const hiddenData1 = document.getElementById('hiddenData1').value;
            

            // Validation
            if (username.length < 3) {
                alert('Username must be at least 3 characters long.');
                return;
            }
            if (phone.length < 10 || phone.length > 20) {
                alert('Phone number must be between 10 and 20 characters long.');
                return;
            }
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailPattern.test(email)) {
                alert('Please enter a valid email address.');
                return;
            }
            if (salary < 5000) {
                alert('Salary must be at least 5000.');
                return;
            }

            const formData = { username, phone, email, salary, hiddenData1 };

            try {
                // Send data to the server using fetch
                const response = await fetch('/cards', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    alert('Thanks for Contacting');
                    window.location.href = '/'; // Redirect to home page
                } else {
                    const errorData = await response.json();
                    alert('Failed to save data: ' + errorData.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('An error occurred. Please try again.');
            }
        };
    </script>
</body>
</html>
