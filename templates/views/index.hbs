<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="/index.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@100..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
<link rel="stylesheet" href="./About.css">

<link rel="icon" href="/logo.png?v=2" >

</head>
<body>
     
    <nav class="navbar">
         <div class="cross-container">
        <i class="fa-solid fa-xmark" id="cancel"></i>
        </div>
        
        <div class="navbar-container">
            
            <div class="main-menu">

        <div class="navbar-container">
            <div class="main-menu">
                <div class="logo">
                    <img src="/navlogo.png" id="logo-img" alt="">
                </div>
                <div class="nav-menu">
                    <ul class="list">
                        {{#each menuItems}}
                            <li class="list-item">
                                <a href="{{url}}" class="navbar-link">{{text}}</a>
                            </li>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    
    <section class="showcase" >
        
          
        <div class="overlay">
             <div class="hamburger-icon-container">
     <i class="fa-solid fa-bars line-icon"></i>
        </div>

        </div>

        <div class="heading-container">
            {{!-- data-aos="fade-up" --}}
            <div class="content" data-aos="fade-up">

                <h2>Best Credit Cards <br> <span> in UAE </span></h2>
                
                <button onclick="window.location.href='/credit-cards'" class="main-btn">Get Now!</button>
            </div>
        </div>
    </section>


  <section class="slider-container" data-aos="fade-up">
<h1>Featured Products</h1>
    <div class="slider-sec">
        
   
     <div class="wrapper">
      <i id="left" class="fa-solid fa-angle-left"></i>
      <div class="carousel">
         <img src="/assets/slider-img/Accor-infinite.png" alt="img" draggable="false">
         <img src="/assets/slider-img/etihad-infinite-card_tcm51-al-h.png" alt="img" draggable="false">
         <img src="/assets/slider-img/manUtdCard_showcase.png" alt="img" draggable="false">
         <img src="/assets/slider-img/skywardsInfinite-nbd.png" alt="img" draggable="false">
         <img src="/assets/slider-img/Al-hilalSmart-platinum-card.jpg" alt="img" draggable="false">
         <img src="/assets/slider-img/cashback-plus-card-1487576128645.jpg" alt="img" draggable="false">

      </div>
      <i id="right" class="fa-solid fa-angle-right"></i>
    </div>
</div>
  </section>

  {{!-- slider ends --}}

  <section class="service-sec" data-aos="fade-up">

       <div class="service-txt">
        <h6>services</h6>
        <h2>Our Services</h2>
    </div>

   
    <div class="service-cards-wrapper">
      
        <div class="service-card">
            <i class="fa-regular fa-credit-card service-logo"></i>
            <h3>Credit Cards</h3>
            <p> We simplify the process of comparing and selecting credit cards, ensuring you find the best options tailored to your lifestyle and financial goals.</p>
        </div>

          <div class="service-card">
            <i class="fa-solid fa-lock service-logo"></i>
            <h3>Security</h3>
            <p>We have a comprehensive privacy policy that outlines how we collect, use, and protect your information. We do not sell or share your personal data with third parties without your explicit consent.</p>
        </div>
          <div class="service-card">
            <i class="fa-solid fa-phone service-logo"></i>
            <h3>Instant Response</h3>
            <p>We are committed to providing a prompt response and will ensure you receive the necessary assistance as quickly as possible. If you have any immediate questions, please feel free to reach out to us.</p>
        </div>

          <div class="service-card">
            <i class="fa-solid fa-user service-logo"></i>
            <h3>Help & Support</h3>
            <p>If you have any questions or need further assistance, please don't hesitate to reach out. We are here to help you with any confusion or concerns you may have. Your satisfaction is our priority.</p>
        </div>
        

    </div>
  </section>

  
<section class="About-sec" data-aos="fade-up">
    <div class="about-container">
        <div class="main-head-sec">
           <img src="/assets/who.jpg" alt="img not found">
        </div>
        <div class="main-head-sec">
            <div class="main-head-dtls about-dtls ">

                 <h1>Why Us ?</h1>
            <p>we specialize in finding the best credit cards tailored to your needs. Our expert team provides comprehensive comparisons and personalized recommendations, ensuring you get the most value and benefits from your credit card choices. With our user-friendly tools and trusted advice, we make the process seamless and straightforward, so you can confidently choose the perfect card for your lifestyle.</p>
            </div>

            <div class="sub-head-sec about-dtls">
                <div class="sec-new">
                    <h2>Who we are ?</h2>
                    <p> we are a passionate team of financial experts dedicated to simplifying the credit card selection process. Our mission is to empower you with the knowledge and tools to make informed financial decisions. With years of experience in the industry, we pride ourselves on offering honest, transparent advice and personalized recommendations. </p>
                </div>
                <div class="sec-new">
                     <h2>We are expert ?</h2>
                    <p>Our team of financial professionals brings a wealth of knowledge and experience to the table. We stay ahead of industry trends and continually research credit card offerings to provide you with the most accurate and up-to-date information. </p>
                </div>

            </div>
            

        </div>
    </div>
    

</section>

{{>footers}}
 
  
    <script>

    const navbar = document.querySelector('.navbar');
const logo = document.querySelector('.logo a');

const hamburgerIcon = document.querySelector('.hamburger-icon-container');
const navMenu = document.querySelector('.nav-menu');
const cancel = document.getElementById("cancel");

hamburgerIcon.addEventListener("click", () => {
    navbar.classList.add("todefault");
});

cancel.addEventListener("click", () => {
    navbar.classList.remove("todefault");
});


        window.addEventListener('scroll', function() {
            if (window.scrollY > 200) {
                navbar.classList.add('scrolled');
                logo.style.color = '#fff'; 
            } else {
                navbar.classList.remove('scrolled');
                logo.style.color = ''; 
            }
        });
        

const carousel = document.querySelector(".carousel"),
firstImg = carousel.querySelectorAll("img")[0],
arrowIcons = document.querySelectorAll(".wrapper i");

let isDragStart = false, isDragging = false, prevPageX, prevScrollLeft, positionDiff;

const showHideIcons = () => {
  
    let scrollWidth = carousel.scrollWidth - carousel.clientWidth; // getting max scrollable width
    arrowIcons[0].style.display = carousel.scrollLeft == 0 ? "none" : "block";
    arrowIcons[1].style.display = carousel.scrollLeft == scrollWidth ? "none" : "block";
}

arrowIcons.forEach(icon => {
    icon.addEventListener("click", () => {
        let firstImgWidth = firstImg.clientWidth + 14; // getting first img width & adding 14 margin value
        // if clicked icon is left, reduce width value from the carousel scroll left else add to it
        carousel.scrollLeft += icon.id == "left" ? -firstImgWidth : firstImgWidth;
        setTimeout(() => showHideIcons(), 60); // calling showHideIcons after 60ms
    });
});

const autoSlide = () => {
    // if there is no image left to scroll then return from here
    if(carousel.scrollLeft - (carousel.scrollWidth - carousel.clientWidth) > -1 || carousel.scrollLeft <= 0) return;

    positionDiff = Math.abs(positionDiff); // making positionDiff value to positive
    let firstImgWidth = firstImg.clientWidth + 14;
    // getting difference value that needs to add or reduce from carousel left to take middle img center
    let valDifference = firstImgWidth - positionDiff;

    if(carousel.scrollLeft > prevScrollLeft) { // if user is scrolling to the right
        return carousel.scrollLeft += positionDiff > firstImgWidth / 3 ? valDifference : -positionDiff;
    }
    // if user is scrolling to the left
    carousel.scrollLeft -= positionDiff > firstImgWidth / 3 ? valDifference : -positionDiff;
}

const dragStart = (e) => {
    // updatating global variables value on mouse down event
    isDragStart = true;
    prevPageX = e.pageX || e.touches[0].pageX;
    prevScrollLeft = carousel.scrollLeft;
}

const dragging = (e) => {
    // scrolling images/carousel to left according to mouse pointer
    if(!isDragStart) return;
    e.preventDefault();
    isDragging = true;
    carousel.classList.add("dragging");
    positionDiff = (e.pageX || e.touches[0].pageX) - prevPageX;
    carousel.scrollLeft = prevScrollLeft - positionDiff;
    showHideIcons();
}

const dragStop = () => {
    isDragStart = false;
    carousel.classList.remove("dragging");

    if(!isDragging) return;
    isDragging = false;
    autoSlide();
}

carousel.addEventListener("mousedown", dragStart);
carousel.addEventListener("touchstart", dragStart);

document.addEventListener("mousemove", dragging);
carousel.addEventListener("touchmove", dragging);

document.addEventListener("mouseup", dragStop);
carousel.addEventListener("touchend", dragStop);

carousel.addEventListener("onclick", function(){
  console.log("js working")
});
    </script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
  AOS.init({
    delay: 350, 
  duration: 500, 
  easing: 'ease',
  });
</script>
  </body>
</html>
</body>
</html>
