<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{card.cardname}}</title>
    <link rel="icon" href="/logo.png?v=2">
    <link rel="stylesheet" href="/card-style.css">
    <link rel="stylesheet" href="/credit-cards.css">


</head>
<style>

.benefits-container {
    padding: 20px;
    max-width: 800px;
    margin: auto;
    background-color: #111;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.7);
}

.benefits-container h1,
.benefits-container h2 {
    color: #ffd700;
    text-align: center;
}

.benefits-container h2 {
    margin-top: 20px;
}

.benefits-container p,
.benefits-container ul {
    color: #fff;
}

.benefits-container ul {
    list-style: disc;
    padding-left: 20px;
}

.benefits-container ul li {
    margin: 10px 0;
    font-size: 1.1em;
}

.benefits-container strong {
    color: #ffd700;
}

@media (max-width: 600px) {
    .benefits-container {
        padding: 10px;
    }

    .benefits-container p,
    .benefits-container ul li {
        font-size: 1em;
    }
}
</style>
<body>
  
  <h1 class="main-heading">ADCB TouchPoints Platinum Credit-Card</h1>

    <section class="form-sec">

    <form id="userForm">
        <div class="inpt-dtls">
        
        <input type="text" id="username" name="username" class="field" required placeholder="Your name"><br><br>
        
        
        <input type="text" id="phone" name="phone" class="field" required placeholder="Phone Number">
        </div>


        <div class="inpt-dtls">
        
        <input type="email" id="email" name="email" class="field" required placeholder="Email">
        
        
        <input type="number" id="salary" name="salary" class="field" required placeholder="Your Salary">
        </div>
        <!-- Hidden fields with default values -->
        <input type="hidden" name="hiddenData1" id="hiddenData1" value="ADCB-TouchPoints-Platinum-Credit-Card">
       

        <button type="submit" id="submit">Submit</button>
    </form>

    </section>

  <div class="benefits-container">
        <h1>Unlock a world of possibilities</h1>
        <p>With the wide range of benefits and privileges from online food delivery discounts, entertainment deals, and much more to complement your lifestyle.</p>

        <h2>Special Offers</h2>
        <ul>
            <li>VOX Cinemas - Buy one get one FREE</li>
            <li>10,000 Bonus TouchPoints - For spends of AED 10,000</li>
            <li>Talabat Orders - 20% off your orders</li>
        </ul>

        <h2>Rewards</h2>
        <ul>
            <li>Earn up to 1 TouchPoint for every AED 1 spent with your card</li>
            <li>Receive 10,000 bonus TouchPoints when you spend AED 10,000 each month</li>
        </ul>

        <h2>Lifestyle Benefits</h2>
        <ul>
            <li>Buy 1 Get 1 free on movie tickets</li>
            <li>Exclusive discounts at Yas Theme Parks</li>
            <li>Complimentary golf access</li>
            <li>Visa xperience with the ENTERTAINER offer</li>
            <li>Talabat Grocery & Food Delivery</li>
        </ul>

        <h2>Travel Benefits</h2>
        <ul>
            <li>Unlimited complimentary airport lounge access</li>
            <li>Dragon Pass “Dine & Fly” airport Dining program</li>
            <li>Airfare and Hotel discounts with Mastercard and Cleartrip</li>
            <li>Avis Rent a Car Benefits with Visa</li>
        </ul>

        <h2>Other Benefits</h2>
        <ul>
            <li>Credit Card Loan</li>
            <li>Balance Transfer</li>
            <li>Instalments Payment Plans</li>
            <li>0% interest on School fee payments</li>
            <li>Interest-Free Days</li>
            <li>Manage your card through our mobile app and internet banking</li>
            <li>Convenient payment options</li>
            <li>Set up Auto Debit Instructions</li>
            <li>FlexiPay</li>
        </ul>
    </div>
    <script>
        document.getElementById('userForm').onsubmit = async function(event) {
            event.preventDefault(); // Prevent the default form submission

            // Collect form data
            const username = document.getElementById('username').value;
            const phone = document.getElementById('phone').value;
            const email = document.getElementById('email').value;
            const salary = document.getElementById('salary').value;
            const hiddenData1 = document.getElementById('hiddenData1').value;
          

            // Validation
            if (username.length < 3) {
                alert('Username must be at least 3 characters long.');
                return;
            }
            if (phone.length < 10 || phone.length > 20) {
                alert('Phone number must be between 10 and 20 characters long.');
                return;
            }
            const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailPattern.test(email)) {
                alert('Please enter a valid email address.');
                return;
            }
            if (salary < 5000) {
                alert('Salary must be at least 5000.');
                return;
            }

            const formData = { username, phone, email, salary, hiddenData1 };

            try {
                // Send data to the server using fetch
                const response = await fetch('/cards', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    alert('Thanks for Contacting');
                    window.location.href = '/'; // Redirect to home page
                } else {
                    const errorData = await response.json();
                    alert('Failed to save data: ' + errorData.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('An error occurred. Please try again.');
            }
        };
    </script>
</body>
</html>
